import React from 'react';
import MenuList from '../Menu/MenuList';
import './Menu.sass';

class Menu extends React.Component {
    render() {
        return (
            <main>
                <MenuList />
            </main>
        );
    }
}

export default Menu;